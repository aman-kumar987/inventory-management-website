<div class="flex items-center justify-center min-h-screen bg-gray-50">
    <div class="w-full max-w-md p-8 space-y-6 bg-white rounded-2xl shadow-xl">
        <h1 class="text-2xl font-bold text-center">Forgot Your Password?</h1>
        <p class="text-center text-sm text-gray-600">Enter your email address and we will send you a link to reset your password.</p>
        <form action="/forgot-password" method="POST" class="space-y-6">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div>
                <label for="email" class="form-label">Email address</label>
                <input type="email" name="email" id="email" required class="form-input mt-1">
            </div>
            <div><button type="submit" class="w-full btn-primary">Send Reset Link</button></div>
        </form>
         <p class="text-center text-sm text-gray-600">Remembered your password? <a href="/login" class="font-medium text-indigo-600 hover:underline">Sign in</a></p>
    </div>
</div>